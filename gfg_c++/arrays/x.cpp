#include <bits/stdc++.h>
using namespace std;

int main(){
    int a[]{1,2,3};
    int sum = 0;
    for(int i=0;i<3;i++)
    {
        sum += (pow(10,i))*a[3-i-1];
    }
    cout<<sum<<endl;

    string s = to_string(sum);
    cout<<s.length()<<endl;

    vector<int> output;
    sort(output.begin(), out)
    output.
    return 0;
}